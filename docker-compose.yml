services:
  blinkin-vlm:
    build: .
    restart: unless-stopped
    env_file:
      - path: .env
        required: false
    environment:
      DEMO_RUNTIME: remote
      HOST: 0.0.0.0
      PORT: 8080
    ports:
      - "${APP_PORT:-8080}:8080"
